# Copy to .env only if your shell loads it; GroundFlip never reads this file itself.
# Prefer setting secrets in your shell or secret manager.
OPENAI_API_KEY=
GROUNDFLIP_OPENAI_MODEL=gpt-5.6-terra
# Optional HMAC key for authenticating certificates. Never commit a real value.
GROUNDFLIP_SIGNING_KEY=

